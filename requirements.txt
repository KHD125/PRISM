# PINNED EXACTLY, DELIBERATELY. Streamlit Cloud rebuilds from this file on every deploy, so a
# floor (>=) means the DEPLOYED engine is not the one the 2,845 tests verified. That is not
# theoretical: until 2026-09-17 this file read `pandas>=2.0.0` while the dev venv held pandas
# 3.0.0 -- a combination pip refuses to build (streamlit 1.54.0 requires pandas<3) -- so the
# suite certified pandas 3 while Cloud quietly ran pandas 2.3.3, across 724 derived columns.
#
# pandas is capped at 2.x by streamlit 1.54.0's own metadata. Moving to pandas 3 requires
# streamlit >= 1.56.0 (the first release allowing pandas<4), which in turn requires migrating the
# 25 `use_container_width` call sites. Bump consciously, suite green first, never as a side effect.
#
# ASCII only, on purpose: this file is parsed by pip on a Linux box with its own locale.
# Pinned and policed by tests/test_dependency_contract.py.

# streamlit==1.54.0
# pandas==3.0.0
# numpy==2.4.2
# openpyxl==3.1.5
# pillow==12.1.0
# plotly==6.5.2

# streamlit==1.54.0
# pandas>=2.0.0
# numpy>=1.24.0
# openpyxl>=3.1.0
# python-calamine>=0.7.0
# pillow>=10.0.0
# plotly>=5.18.0
# streamlit-aggrid>=0.3.15

# PINNED EXACTLY. Cloud rebuilds from this file on every deploy, so a floor (>=) means the
# deployed engine is NOT the one the tests verified. pandas is capped at 2.x by streamlit
# 1.54.0's own metadata (pandas<3) — see tests/test_dependency_contract.py.


streamlit==1.54.0
pandas==2.3.3
numpy==2.4.2
openpyxl==3.1.5          # RUNTIME-ONLY: _xlsx_engine() names it as a STRING, never imports it
python-calamine==0.7.0
pillow==12.1.0
plotly==6.5.2
requests==2.32.5         # imported inside a function at core/sheet_meta.py:84
